server:
  port: 8000

spring:
  application:
    name: api-gateway
  cloud:
    gateway:
      routes:
        - id: customer-service
          uri: lb://customer-service
          predicates:
            - Path=/customer/**
        - id: loan-service
          uri: lb://loan-service
          predicates:
            - Path=/loan/**

eureka:
  client:
    service-url:
      defaultZone: http://discovery-service:8761/eureka

